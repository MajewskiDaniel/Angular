"use strict";
let myPet = "dog";
const myColor = 'Green';
function isDog(text) {
    return text == 'dog';
}
;
const isCat = (text) => text == 'cat';
console.log(isDog(myPet));
console.log(isCat(myPet));
class MyClass {
    test() { }
}
